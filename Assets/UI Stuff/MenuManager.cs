using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Rendering;

public class MenuManager : MonoBehaviour
{
    public static bool MenuOpen = false;

    private void Update()
    {
        /*
        if (MenuOpen)
            gameObject.GetComponentInChildren<Volume>().enabled = true;
        else
            gameObject.GetComponentInChildren<Volume>().enabled = false;
        */
    }
}
